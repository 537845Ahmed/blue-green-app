apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: LoadBalancer
  selector:
    app: myapp
    version: blue   # 🔄 verander naar "green" om over te schakelen
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080